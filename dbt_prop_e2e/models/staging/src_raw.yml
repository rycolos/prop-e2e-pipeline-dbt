version: 2

sources:
  - name: raw
    database: prop-e2e-dbt
    schema: raw
    tables:
      - name: psk
        columns:
          - name: sender_callsign
            tests:
              - not_null
          - name: receiver_callsign
            tests:
              - not_null
      - name: logbook
        columns:
          - name: sender_callsign
            tests:
              - not_null
          - name: receiver_callsign
            tests:
              - not_null